# Frontend (React)

Single-page React app that renders a centered **Hello World** heading with a minimal light theme.

## Run locally

```bash
npm install
npm start
```

The app runs on port **3000** by default (or `REACT_APP_PORT` if your environment maps it accordingly).
