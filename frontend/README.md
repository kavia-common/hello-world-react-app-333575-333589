# Frontend (React)

Single-page React UI that renders a centered **"Hello World"** heading with a minimal light theme.

## Run locally

```bash
npm install
npm start
```

The dev server runs on port **3000** (see `.env` / `REACT_APP_PORT`).
