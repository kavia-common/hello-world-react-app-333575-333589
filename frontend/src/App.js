import React from 'react';
import StreamVibeApp from './StreamVibeApp';

/**
 * Root App component – renders the StreamVibe Shows Page.
 * The StreamVibeApp component implements the full "Shows Page Open – Laptop"
 * Figma design (node 2005:2032).
 * PUBLIC_INTERFACE
 */
function App() {
  return <StreamVibeApp />;
}

export default App;
